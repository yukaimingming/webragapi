# .NET 构建产物
bin/
obj/
*.user
*.suo

# Visual Studio / Rider 缓存
.vs/
.idea/

# 本地运行数据（不进仓库）：向量库数据文件、上传的文档
qdrant_storage/
App_Data/

# 临时文件
*.log

# Tesseract 英文模型当前未使用（OCR 只用 chi_sim），不入库
tessdata/eng.traineddata

# 客户端更新包体积较大，生产环境由文件服务器或 WebAPI 静态目录提供，不提交到 Git。
wwwroot/updates/*.zip
